apiVersion: v1
kind: Secret
metadata:
  name: registry-credentials
type: kubernetes.io/dockerconfigjson
data:
  # This is generated with `make gitlab-pull-secret`
  # If the output contains a line break or other whitespace, remove it.
  .dockerconfigjson: "CHANGE ME"
