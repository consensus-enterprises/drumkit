apiVersion: batch/v1
kind: Job
metadata:
  name: job-install-drupal
spec:
  template:
    spec:
      containers:
        - name: job-install-drupal
          image: {{ DRUPAL_CONTAINER_REGISTRY_URL }}/drupal:{{ DRUPAL_CONTAINER_IMAGE_TAG }}
