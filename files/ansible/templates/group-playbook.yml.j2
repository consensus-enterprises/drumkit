---
- name: Group playbook for group '{{ group }}' generated by Drumkit.
  hosts: {{ group }}
  become: true
  gather_facts: True

  #roles:
    # Invoke some roles, e.g.:
    #- consensus.utils
    #- consensus.admin-users
