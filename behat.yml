default:
  suites:
    default:
      paths:
        - "%paths.base%/features/bootstrap"
        - "%paths.base%/mk/tasks"
        - "%paths.base%/mk/projects"
      contexts:
        - Drumkit\DrumkitContext
  gherkin:
    filters:
      tags: "~@wip&&~@slow&&~@disabled&&~@noisy"

