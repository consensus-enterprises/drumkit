default:
  suites:
    default:
      paths:
        - "%paths.base%/features/bootstrap"
      contexts:
        - Drumkit\DrumkitContext
  gherkin:
    filters:
      tags: "~@wip&&~@slow&&~@disabled&&~@noisy"

