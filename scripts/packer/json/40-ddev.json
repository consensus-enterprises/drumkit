{
  "builders": [
    {
      "image": "{{user `repo_url`}}/docker:latest",
      "type": "docker",
      "privileged": "true",
      "commit": "true"
    }
  ],
  "post-processors": [
    [
      {
        "repository": "{{user `repo_url`}}/{{user `image_name`}}",
        "tags": [
          "{{user `ubuntu_version`}}-1"
        ],
        "type": "docker-tag"
      },
      {
        "repository": "{{user `repo_url`}}/{{user `image_name`}}",
        "tags": [
          "latest"
        ],
        "type": "docker-tag"
      },
      {
        "type": "docker-push"
      }
    ]
  ],
  "provisioners": [
    {
      "scripts": [
        "build/packer/scripts/ddev.sh"
      ],
      "type": "shell"
    },
    {
      "scripts": [
        "build/packer/scripts/cleanup.sh"
      ],
      "type": "shell"
    }

  ],
  "variables": {
    "image_name": "docker",
    "ubuntu_version": "20.04",
    "repo_url": "{{env `CONTAINER_REGISTRY_URL`}}"
  }
}
