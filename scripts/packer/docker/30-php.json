{
  "builders": [
    {
      "type": "docker",
      "image": "registry.gitlab.com/consensus.enterprises/clients/hc/controlled-vocabulary/base:latest",
      "commit": true,
      "pull": false
    }
  ],
  "provisioners": [
    {
      "type": "shell",
      "scripts": [
        "scripts/packer/scripts/php.sh",
        "scripts/packer/scripts/cleanup.sh"
      ]
    }
  ],
  "post-processors": [
    [
      {
        "type": "docker-tag",
        "repository": "registry.gitlab.com/consensus.enterprises/clients/hc/controlled-vocabulary/{{user `image_name`}}",
        "tag": "0.0.x"
      },
      {
        "type": "docker-tag",
        "repository": "registry.gitlab.com/consensus.enterprises/clients/hc/controlled-vocabulary/{{user `image_name`}}",
        "tag": "latest"
      },
      {
        "type": "docker-push"
      }
    ]
  ],
  "variables": {
    "image_name": "php"
  }
}
